{% extends 'base.html.twig' %}

{% block title %}Email de réinitialisation envoyé{% endblock %}

{% block body %}
<div class="max-w-md mx-auto mt-12 px-4 sm:px-6">
    <div class="bg-white shadow rounded-lg overflow-hidden">
        <div class="px-6 py-8 text-center">
            <h1 class="text-2xl font-bold text-gray-900 mb-6">Email de réinitialisation envoyé</h1>

            <p class="text-gray-600 mb-4">
                Si un compte correspondant à votre adresse e-mail existe, un e-mail vient de vous être envoyé avec un lien que vous pouvez utiliser pour réinitialiser votre mot de passe.
            </p>
            <p class="text-gray-600 mb-6">
                Ce lien expirera dans
                <span class="font-semibold">{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}</span>.
            </p>

            <p class="text-gray-600 mb-8">
                Si vous ne recevez pas d'email, veuillez vérifier votre dossier spam ou
                <a href="{{ path('app_forgot_password_request') }}" class="text-yb-dark hover:text-yb-gold font-medium">réessayez</a>.
            </p>

            <a href="{{ path('app_login') }}" class="inline-flex items-center justify-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-yb-dark hover:bg-opacity-90 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-yb-gold">
                Retour à la page de connexion
            </a>
        </div>
    </div>
</div>
{% endblock %}