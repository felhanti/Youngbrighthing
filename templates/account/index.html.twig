{% extends 'base.html.twig' %}

{% block title %}
	Bienvenue
{% endblock %}

{% block body %}
	<div class="container my-5 p-3">
		<h1 class="my-3 text-center">
			Back-office de YOUNGBRIGHTHING
		</h1>
		<div class="d-grid gap-2 col-4 mx-auto">
			<a href="{{ path('order_list')}}" class="btn btn-primary">Mes information</a>
			<a href="{{ path('order_list')}}" class="btn btn-primary">Mes commandes</a>
		</div>
	</div>
{% endblock %}
