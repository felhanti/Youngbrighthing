{% extends 'base.html.twig' %}

{% block title %}
	{{ category.Name }}
{% endblock %}

{% block body %}
	<div class="container mt-4">
		<h1>{{ category.Name }}</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ category.id }}</td>
				</tr>
				<tr>
					<th>Name</th>
					<td>{{ category.Name }}</td>
				</tr>
			</tbody>
		</table>

		<a class="btn btn-info mr-1" href="{{ path('app_admin_category_index') }}">Retour à la liste</a>

		<a class="btn btn-warning m-1" href="{{ path('app_admin_category_edit', {'id': category.id}) }}">Modifier</a>

		{{ include('admin/category/_delete_form.html.twig') }}
	{% endblock %}
