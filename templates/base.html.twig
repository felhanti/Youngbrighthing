<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{% endblock %} - Youngbrighthing</title>
    <link rel="icon" href="{{ asset('favicon.ico') }}">
    
    <!-- Tailwind CSS via CDN -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    
    <!-- Fonts Google -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Styles personnalisés supplémentaires -->
    <style>
        /* Palette de couleurs personnalisée */
        :root {
            --color-gold: #FFD700;
            --color-dark: #1A1A1A;
            --color-light: #F8F8F8;
            --color-gray: #C7C7C7;
        }
        
        /* Police personnalisée */
        body {
            font-family: 'Inter', sans-serif;
        }
        
        h1, h2, h3, h4, h5, h6 {
            font-family: 'Playfair Display', serif;
        }
        
        /* Couleurs personnalisées */
        .bg-yb-gold { background-color: var(--color-gold); }
        .bg-yb-dark { background-color: var(--color-dark); }
        .bg-yb-light { background-color: var(--color-light); }
        .bg-yb-gray { background-color: var(--color-gray); }
        
        .text-yb-gold { color: var(--color-gold); }
        .text-yb-dark { color: var(--color-dark); }
        .text-yb-light { color: var(--color-light); }
        .text-yb-gray { color: var(--color-gray); }
        
        .border-yb-gold { border-color: var(--color-gold); }
        .border-yb-dark { border-color: var(--color-dark); }
        
        /* Composants personnalisés */
        .btn-primary {
            background-color: var(--color-dark);
            color: var(--color-gold);
            padding: 0.5rem 1rem;
            border-radius: 0.375rem;
            transition: opacity 0.2s;
        }
        
        .btn-primary:hover {
            opacity: 0.9;
        }
        
        .btn-secondary {
            background-color: var(--color-gray);
            color: var(--color-dark);
            padding: 0.5rem 1rem;
            border-radius: 0.375rem;
            transition: opacity 0.2s;
        }
        
        .btn-secondary:hover {
            opacity: 0.9;
        }
    </style>
    
    {% block stylesheets %}{% endblock %}
    
    <!-- Alpine.js via CDN pour les interactions légères -->
    <script src="https://cdn.jsdelivr.net/npm/alpinejs@3.13.1/dist/cdn.min.js" defer></script>
    
    {% block javascripts %}{% endblock %}
</head>
<body class="flex flex-col min-h-screen bg-gray-100 text-gray-900">
    <header>
        <!-- Navigation principale -->
        <nav class="bg-white shadow-sm">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex justify-between h-20">
                    <!-- Logo et Burger Menu -->
                    <div class="flex items-center">
                        <button class="lg:hidden p-2 rounded-md text-gray-400 hover:text-gray-900 focus:outline-none" 
                                x-data="{}" 
                                @click="document.getElementById('mobileMenu').classList.toggle('hidden')" 
                                aria-expanded="false">
                            <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                            </svg>
                        </button>
                        <a href="{{ path('home') }}" class="flex-shrink-0 flex items-center">
                            <img class="h-14 w-auto" src="{{ asset('images/LOGO_ybt-01_72c34f78-1ef2-4c5c-a378-735dffc185cb.png') }}" alt="Youngbrighthing">
                        </a>
                    </div>
                    
                    <!-- Menu Icons -->
                    <div class="flex items-center space-x-4">
                        {% if app.user %}
                            {% if 'ROLE_ADMIN' in app.user.roles %}
                                <a href="{{ path('app_admin_dashboard') }}" class="text-gray-500 hover:text-gray-900 transition">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                                    </svg>
                                </a>
                            {% endif %}
                            
                            <a href="{{ path('app_account') }}" class="text-gray-500 hover:text-gray-900 transition">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                                </svg>
                            </a>
                            
                            <a href="{{ path('app_logout') }}" class="text-gray-500 hover:text-gray-900 transition">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                                </svg>
                            </a>
                            
                            {% if app.user.carts|first %}
                                <a href="{{ path('app_cart_show', {'id': app.user.carts|first.id}) }}" class="text-gray-500 hover:text-gray-900 transition relative">
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z" />
                                    </svg>
                                    <span class="absolute -top-2 -right-2 bg-yb-gold text-yb-dark rounded-full w-5 h-5 flex items-center justify-center text-xs font-bold">
                                        {{ app.user.carts|first.product|length }}
                                    </span>
                                </a>
                            {% endif %}
                        {% else %}
                            <a href="{{ path('app_login') }}" class="text-gray-500 hover:text-gray-900 transition">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1" />
                                </svg>
                            </a>
                        {% endif %}
                    </div>
                </div>
            </div>
            
            <!-- Menu mobile -->
            <div class="hidden lg:hidden" id="mobileMenu">
                <div class="px-2 pt-2 pb-3 space-y-1">
                    <a href="{{ path('app_collection_all') }}" class="block px-3 py-2 rounded-md text-base font-medium text-gray-900 hover:bg-gray-200">Collections</a>
                    <a href="{{ path('app_campagne') }}" class="block px-3 py-2 rounded-md text-base font-medium text-gray-900 hover:bg-gray-200">Campagne</a>
                    <a href="{{ path('app_histoire') }}" class="block px-3 py-2 rounded-md text-base font-medium text-gray-900 hover:bg-gray-200">Histoire</a>
                </div>
            </div>
        </nav>
        
        <!-- Menu principal desktop -->
        <nav class="bg-yb-dark text-yb-gold hidden lg:block">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex justify-center space-x-24 h-16">
                    <a href="{{ path('app_collection_all') }}" class="flex items-center px-3 py-2 text-xl font-medium hover:opacity-80 transition">Collections</a>
                    <a href="{{ path('app_campagne') }}" class="flex items-center px-3 py-2 text-xl font-medium hover:opacity-80 transition">Campagne</a>
                    <a href="{{ path('app_histoire') }}" class="flex items-center px-3 py-2 text-xl font-medium hover:opacity-80 transition">Histoire</a>
                </div>
            </div>
        </nav>
    </header>

    <main class="flex-grow">
        {% for label, messages in app.flashes %}
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
                {% for message in messages %}
                    <div class="rounded-md p-4 {% if label == 'success' %}bg-green-100 text-green-800{% elseif label == 'danger' or label == 'error' %}bg-red-100 text-red-800{% else %}bg-blue-100 text-blue-800{% endif %}" role="alert">
                        <p class="text-sm font-medium">{{ message }}</p>
                    </div>
                {% endfor %}
            </div>
        {% endfor %}
        
        {% block body %}{% endblock %}
    </main>

    <footer class="bg-yb-dark text-white mt-12">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                <!-- Informations légales -->
                <div>
                    <h3 class="text-yb-gold text-lg font-semibold mb-4">Informations légales</h3>
                    <ul class="space-y-2">
                        <li><a href="{{ path('app_cgv_cgu') }}" class="hover:text-yb-gold transition">CGU</a></li>
                        <li><a href="{{ path('app_cgv_confidentialite') }}" class="hover:text-yb-gold transition">Confidentialité</a></li>
                        <li><a href="{{ path('app_cgv_mentions_legales') }}" class="hover:text-yb-gold transition">Mentions légales</a></li>
                        <li><a href="{{ path('app_cgv_retour') }}" class="hover:text-yb-gold transition">Conditions de retour</a></li>
                    </ul>
                </div>
                
                <!-- Suivez-nous -->
                <div>
                    <h3 class="text-yb-gold text-lg font-semibold mb-4">Suivez-nous</h3>
                    <a href="https://www.instagram.com/youngbrighthing/" target="_blank" class="inline-block mb-3">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-white hover:text-yb-gold transition" fill="currentColor" viewBox="0 0 24 24">
                            <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
                        </svg>
                    </a>
                    <p class="text-sm">Retrouvez-nous sur Instagram</p>
                </div>
            </div>
            
            <div class="text-center text-yb-gold text-sm mt-8 pt-8 border-t border-gray-700">
                &copy; {{ "now"|date("Y") }} Youngbrighthing. Tous droits réservés.
            </div>
        </div>
    </footer>
</body>
</html>